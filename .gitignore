node_modules/
dist/

# The raw workbook export carries all 14 sheets, including internal-only ones.
# This repo is public: the export never enters it. To regenerate the JSON,
# place the export at data/raw/ locally and run: npm run build-data
data/raw/

# Client working notes, tracked outside the public repo.
DATA-QUESTIONS.md

# Actual DER counts remain internal (Irene's ruling on KJ's Q4/Q10 answers,
# 2026-08-30). These two snapshots carry the counts, so they stay local;
# the dashboard imports only regions.json and the homeowner profile.
data/der-concentration.json
data/community-need-index.json

# Local editor and OS noise
.DS_Store
*.local

# StatCan boundary source: 21.7 MB zip in, 55 KB of Nova Scotia paths out.
# Only the trimmed output ships. Re-run: python scripts/build-geo.py
data/geo-src/
